@model IEnumerable<ClassicComedians.Models.ViewModels.ComedianListViewModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GroupName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ComedianId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ComedianId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ComedianId })
            </td>
        </tr>
}
    </tbody>
</table>
